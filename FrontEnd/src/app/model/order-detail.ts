import { NumberValueAccessor } from "@angular/forms";

export class OrderDetail {
    id!: number;
    productDetailId!: number;
    quantity!: number;
    orderId!: number;
    productId!: number;
}
